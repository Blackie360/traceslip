import { expect, test } from "@playwright/test"
test("public trust story and navigation",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Paper fades/i})).toBeVisible();await expect(page.getByText("AI never auto-saves")).toBeVisible();await expect(page.getByRole("link",{name:/Create your workspace/i})).toHaveAttribute("href","/sign-up")})
test("auth pages expose accessible inputs",async({page})=>{await page.goto("/sign-up");await expect(page.getByLabel("Full name")).toBeVisible();await expect(page.getByLabel("Email address")).toBeVisible();await expect(page.getByLabel("Password")).toBeVisible()})
test("verification page explains the next step",async({page})=>{await page.goto("/verify-email");await expect(page.getByRole("heading",{name:"Verify your email"})).toBeVisible();await expect(page.getByText("Verification email sent")).toBeVisible();await expect(page.getByRole("list",{name:"Email verification steps"})).toBeVisible();await expect(page.getByRole("link",{name:"Go to sign in"})).toHaveAttribute("href","/sign-in")})
