import { BrandMark } from "@/components/brand-mark"

export function AuthShell({ eyebrow, title, copy, children, footer }: { eyebrow: string; title: string; copy: string; children: React.ReactNode; footer?: React.ReactNode }) {
  return <main className="grid min-h-screen lg:grid-cols-[.9fr_1.1fr]"><section className="flex flex-col bg-card p-6 sm:p-10 lg:p-14"><BrandMark/><div className="my-auto mx-auto w-full max-w-md py-16"><p className="ledger-label text-accent">{eyebrow}</p><h1 className="mt-4 text-4xl font-semibold tracking-[-.05em]">{title}</h1><p className="mt-3 leading-7 text-muted-foreground">{copy}</p><div className="mt-8">{children}</div>{footer && <div className="mt-8 text-sm text-muted-foreground">{footer}</div>}</div></section><aside className="relative hidden overflow-hidden bg-[#201e1a] p-14 text-white lg:block"><div className="absolute inset-0 opacity-10" style={{ backgroundImage: "repeating-linear-gradient(0deg, transparent, transparent 27px, white 28px)" }}/><div className="relative flex h-full flex-col justify-between"><p className="font-mono text-xs uppercase tracking-[.2em] text-orange-400">Trust is a workflow</p><blockquote className="max-w-xl text-5xl font-semibold leading-[1.05] tracking-[-.055em]">“The original stays beside the record. Every suggestion shows its evidence. Final means final.”</blockquote><p className="font-mono text-xs text-white/45">TRACESLIP / CONTROLLED DIGITAL COPIES</p></div></aside></main>
}
